{
  "name": "collaflow-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "CollaFlow — 高性能协同脑图框架，支持 MCP 协议与私有化部署",
  "license": "Apache-2.0",
  "type": "commonjs",
  "engines": {
    "node": ">=20",
    "pnpm": ">=8"
  },
  "scripts": {
    "build": "pnpm --filter \"@collaflow/*\" run build",
    "dev": "pnpm --parallel --filter \"@collaflow/*\" run dev",
    "dev:collacore": "pnpm --filter @collaflow/collacore run dev",
    "test": "pnpm --filter \"@collaflow/*\" run test",
    "test:watch": "pnpm --parallel --filter \"@collaflow/*\" run test:watch",
    "typecheck": "pnpm --filter \"@collaflow/*\" run typecheck",
    "clean": "pnpm --filter \"@collaflow/*\" run clean",
    "skills:validate": "node scripts/skill-governance.mjs",
    "commit": "cz",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish",
    "prepare": "husky"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog",
      "scopes": [
        "collacore",
        "collamind",
        "collamarkdown",
        "root",
        "deps",
        "ci",
        "release",
        "skills"
      ]
    }
  },
  "pnpm": {
    "overrides": {
      "human-id": "4.1.1"
    }
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "@types/node": "^20.17.6",
    "commitizen": "^4.3.2",
    "cz-conventional-changelog": "^3.3.0",
    "husky": "^9.1.7",
    "js-yaml": "^4.1.0",
    "ajv": "^8.17.1",
    "lint-staged": "^15.5.2",
    "rimraf": "^5.0.10",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
